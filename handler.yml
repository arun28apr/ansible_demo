---
 - name: To check handler module
   hosts: git_node
   become: true
   tasks:
    
     - name: going to check handler now
       command: echo "restart the services"
       notify: "restart the service"

   handlers:
     - name: restart the service
       service: name=sshd state=restarted
       register: p6
     - debug: var=p6
